local StarterGui = game:GetService("StarterGui")

local function sendSystemNotification(title, msg, duration)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = msg,
            Duration = duration or 5,
            Button1 = "OK"
        })
    end)
end

local games = {
    [6739698191] = "https://raw.githubusercontent.com/ranz-coder/roblox-sc/refs/heads/main/LaPer-HUB.lua"
}

local currentPlaceId = game.PlaceId
local targetScriptURL = games[currentPlaceId]

if targetScriptURL then
    sendSystemNotification("LaperGank System", "Map terdeteksi! Mengunduh LaPer-HUB...", 3)
    
    -- Mengambil data script utama dari cloud storage GitHub
    local success, scriptContent = pcall(function()
        return game:HttpGet(targetScriptURL)
    end)
    
    if success and scriptContent then
        -- Mengonversi raw text menjadi executable environment
        local executeScript, err = loadstring(scriptContent)
        if executeScript then
            executeScript()
            sendSystemNotification("Laper Gank Panel", "LaPer-HUB Berhasil Dijalankan!", 3)
        else
            sendSystemNotification("Loader Error", "Gagal memproses kode: " .. tostring(err), 5)
        end
    else
        sendSystemNotification("Connection Error", "Gagal mengambil data dari repositori ranz-coder!", 5)
    end
else
    -- Penanganan perlindungan jika dijalankan di luar map Violence District
    sendSystemNotification("LaperGank System", "LaperGank gak mendukung map ini!", 5)
end
